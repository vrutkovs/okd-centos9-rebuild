apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: cluster-kube-apiserver-operator
spec:
  source:
    type: Git
    git:
      uri: 'https://github.com/vrutkovs/cluster-kube-apiserver-operator'
      ref: 4.12-watch-caches
    contextDir: .
    images: []
  strategy:
    type: Docker
    dockerStrategy:
      imageOptimizationPolicy: SkipLayers
      dockerfilePath: Dockerfile.rhel7
      from:
        kind: "ImageStreamTag"
        name: "release:base"
  output:
    to:
      kind: ImageStreamTag
      name: 'release:cluster-kube-apiserver-operator'
